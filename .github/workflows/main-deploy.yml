on:
    push:
        branches:
            - main

jobs:
    build-project:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello to world
              run: |
                  echo "Hello world"
